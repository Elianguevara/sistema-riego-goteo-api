spring.application.name=sistema-riego-goteo-api

# ===================================================================
# Configuraci贸n del Servidor
# ===================================================================
server.port=8080

# ===================================================================
# Configuraci贸n de la Base de Datos (MySQL en Google Cloud SQL)
# ===================================================================
# Opci髇 A: Usar la zona horaria del sistema donde corre la API
spring.datasource.url=jdbc:mysql://localhost:3306/sistema_riego?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=America/Argentina/Buenos_Aires

# Opci髇 B: Si prefieres que coincida con el sistema operativo simplemente quita el par醡etro o usa:
# serverTimezone=GMT-3
spring.datasource.username=root
spring.datasource.password=Chack689
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===================================================================
# Configuraci贸n de JPA e Hibernate
# ===================================================================
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ===================================================================
# Configuraci贸n de JWT (JSON Web Token)
# ===================================================================
jwt.secret=d1UZcMxQ/1rHinnQ2jnSeyrXaM12fS4d5Cw/lZJnFfHD7bItRtB9XjghV5ju4XYV86ZKEiwy9A4vIqH0LBnDPg==
jwt.expiration=170000000

# ===================================================================
# Configuraci贸n de la API de Clima y Geolocalizaci贸n
# ===================================================================
weather.api.key=bdf490c255356bad93a835bba58bae47
weather.api.url=https://api.openweathermap.org/data/2.5/weather

geocoding.api.key=88e0922ebdfd463cb4f2d8116f7f7eca
geocoding.api.url=https://api.opencagedata.com/geocode/v1/json

# ===================================================================
# Logs
# ===================================================================
logging.level.org.springframework.security=DEBUG

# ===================================================================
# Seguridad y CORS (NUEVO)
# ===================================================================
# Lista de or铆genes permitidos separados por coma.
# En producci贸n, cambia esto por el dominio real (ej. https://mi-app-riego.com)
cors.allowed-origins=http://localhost:4200,http://localhost:5173